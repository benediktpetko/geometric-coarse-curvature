import numpy as np


def triangular_parallel_transport_distance(
        root: np.ndarray, target, graph_distances: np.ndarray, midpoints: np.ndarray, scale: float):
    pass


def triangular_wasserstein_distance(
        root: np.ndarray, target, graph_distances: np.ndarray, midpoints: np.ndarray, scale: float):
    pass
